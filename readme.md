💰 Expense Tracker - Desktop Application

A full-stack **Expense Tracker System** built with **Java Spring Boot** and **React**, packaged as a standalone portable Desktop Application (`.exe`). This application runs without requiring XAMPP, MySQL server, or Node.js installation on the client's machine.


## 🚀 Features
* **Simple & Clean UI:** User-friendly interface built with React.
* **Add & Manage Expenses:** Easily record daily expenses.
* **Portable Desktop App:** Runs as a single `.exe` file.
* **No Setup Required:** Uses an embedded H2 database (file-based storage), so no external database installation is needed.
* **Data Persistence:** Data is saved locally in a `data` folder on the user's machine.

## 🛠️ Tech Stack
* **Frontend:** React.js, Vite, Axios
* **Backend:** Java, Spring Boot 3
* **Database:** H2 Database (File Mode)
* **Build Tool:** Maven
* **Packaging:** Launch4j (Converted JAR to EXE)

---

## 📥 How to Run (For Users)
You don't need any coding knowledge to run this app.

1.  Download the **`ExpenseTrackerSoftware.zip`** file from the Releases section.
2.  Extract the Zip file.
3.  Double-click on **`MyExpenseApp.exe`**.
4.  The application will open in your default browser automatically.
5.  All your data will be saved securely in the `data` folder created on your desktop.

---

## 👨‍💻 Developer Setup (Source Code)
If you want to modify or run the source code, follow these steps:

### Prerequisites
* Java JDK 17+
* Node.js & npm
